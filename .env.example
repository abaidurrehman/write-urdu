# Local-only secrets for scripts run from your machine (e.g. scripts/ai-writing-benchmark/run.js).
# Copy to .env and fill in real values. .env is gitignored — never commit it.
# These are the SAME values already stored as Pages secrets (wrangler pages secret put ...);
# wrangler secrets are write-only, so local scripts need their own copy here.

CF_ACCOUNT_ID=852591536ef10f44e5e87c0cb08c9e82
CF_AIG_GATEWAY=write-urdu-ai
CF_AIG_TOKEN=
MISTRAL_API_KEY=
CEREBRAS_API_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=
