# Write Urdu UI logo source

The rendered UI logo must preserve the complete mark in `image/logo.svg` rather than a cropped raster conversion. The shared header/footer may use `assets/brand/logo-ui.svg`; `image/logo10.png` remains the compatibility PNG used by existing schema/export references.
