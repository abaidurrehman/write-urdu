# Brand assets

`logo-ui.svg` is the compact vector UI mark used to preserve the full Write Urdu logo shape at small header/footer sizes. `image/logo10.png` remains the compatibility PNG used by existing metadata and legacy surfaces.
