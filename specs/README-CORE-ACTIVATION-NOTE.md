# Core activation governance note

`WU-PLAT-002H` is the current P0 product/UX acceptance slice and should be treated as a roadmap gate before new major feature work.

It is deliberately separate from the canonical registry (`specs/README.md`) only until the registry is updated in the same branch. The canonical entry should identify:

- `WU-PLAT-002H` — Core Activation & Feature Discovery Acceptance — **Active / P0**;
- parent `WU-PLAT-002`;
- commercial owner `WU-GROWTH-001`;
- implementation checklist `WU-PLAT-002H-IMPLEMENTATION-CHECKLIST.md`;
- evidence record `docs/WU-CORE-ACTIVATION-EVIDENCE-2026-08-30.md`.

This temporary note may be removed once the canonical registry/backlog edits are committed.
