# WU Core Activation Conversion Repair

Canonical execution skill:

```text
skills/core-activation-conversion/SKILL.md
```

When this skill is selected, read the canonical skill in full and follow its mandatory slice order, privacy rules, acceptance gates and PR format.

Start with **Slice 0 only** unless the user explicitly asks for a later slice and the earlier gates are already satisfied in the current repository state.

Do not duplicate or reinterpret the canonical instructions in this wrapper.